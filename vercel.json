{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "docs/.vitepress/dist",
          "buildCommand": "pnpm install --no-frozen-lockfile && pnpm docs:build"
        }
      }
    ],
    "cleanUrls": true
  }